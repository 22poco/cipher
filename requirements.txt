# Python dependencies for the cipher backend (FastAPI).
# Install from the repo root: pip install -r requirements.txt
#
# The frontend (Next.js) is a separate npm project and cannot share this file
# — its dependencies live in frontend/package.json. Install them with:
#   cd frontend && npm install

fastapi
uvicorn[standard]
psycopg[binary]
python-dotenv
SQLAlchemy
bcrypt
PyJWT[crypto]
email-validator
